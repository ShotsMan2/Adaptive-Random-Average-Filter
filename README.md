# Adaptive-Random-Average-Filter
A C-based statistical algorithm that generates a random dataset within a fixed range [0-100]. It computes the arithmetic mean and iteratively regenerates any data points falling below the average until the entire set exceeds the calculated threshold.
